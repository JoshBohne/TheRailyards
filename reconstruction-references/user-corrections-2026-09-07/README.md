# Explicit right-field / tower corrections

User instructions relayed from the coordinating task on September 7:

1. The low arched/glazed building in front of the right-field grandstand projects too far. Its actual facade, roof/deck edge and main-building connection must align flush as shown in `source-flush-frontage.png`. Do not fix only a cosmetic overhang.
2. Move the clock tower toward the river: its side nearest home plate must align with the right-field foul pole. Verify the footprint/near face, not the center.
3. Show a fixed comparison proving the flush frontage and an overhead view proving tower-face/foul-pole alignment; keep the generator and saved scene consistent.

`current-projecting-frontage.png` and `current-tower-plan.png` document the rejected current geometry. `source-flush-frontage.png` and `source-tower-foul-pole.png` show the intended relationship. These are retained user crops; full official renderings remain available in the parent reference directory.
