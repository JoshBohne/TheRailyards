# V15 bounded visual review

Historical intermediate review; predates the user's freestanding-arch and open-terrace clarification. Finding 3 is superseded by that clarification. See V15-DELIVERY.md for the final checkpoint.

Prepared 2026-09-08 from the saved-scene preview. This is a bounded review of the requested seating, tier, tunnel and bridge-comparison views; it is not a 23-finding audit.

Inspected source references `work/v15/review/source-south.jpg`, `source-north.jpg`, and `source-bridge.jpg`, plus `after-lf.png`, `after-rf.png`, `after-concourse.png`, `after-home.png`, `after-north.png`, and `after-bridge.png`. All six after-view receipts report scene SHA-256 `0e86b123d913ae67230f8726c215a95d1e726a7812c8c48565385e46d4e1d01b`.

## Actionable findings

1. **RF return still has a hard faceted break and an unsupported-looking concourse junction.** In `after-rf.png`, the straight rows are readable within each bank, but the lower return ends in a broad diagonal wedge and the upper-left concourse slab bridges over a dark void with no visible column, wall or raker. The tier rhythm does not continue around the corner as the continuous swept bowl in `source-north.jpg` does. Re-fit the row endpoints and return edge while preserving straight individual rows, then connect that slab to a plausible support datum.

2. **LF bank still cuts off at the foul-pole-side end.** In `after-lf.png`, the final lower-bank rows meet a large blank slab/vertical edge at the foul-pole return, with no convincing landing or continuous perimeter guard. The upper return continues past this visual cut, so the bank reads as an abrupt termination rather than one connected seating structure. Reconcile the last rows, coping and landing against the source return before treating the LF pass as closed.

3. **The lower Roosevelt arrival is not visually legible as a tunnel route.** In `after-concourse.png`, the new arches read as nearly black voids: the floor grade, handrails and far-side continuation are not visible, while the scoreboard/terrace edge occludes the left side of the arrival. Add a clear lit floor and one uninterrupted sightline through the intended passage, then verify that the visible upper terrace actually feeds the arches.

4. **Rendered tier count remains difficult to reconcile with the source.** `after-rf.png` exposes many narrow repeated shelf/seat bands, while `source-north.jpg` resolves fewer, broader primary seating decks separated by clear concourses. Record the source tier count and identify which current bands are seating versus concourse/guard edges; merge or re-space any extra bands that are not source-supported.

## Checks and pending geography work

- The colored pinwheels are visible on the centerfield scoreboard in `after-home.png` and `after-north.png`; no new pinwheel defect was found.
- The adjusted bridge frame `after-bridge.png` visibly includes the powerhouse as the white industrial building with twin stacks in the lower-right foreground.
- The mapped river bend, Willis right-bank relation and southern rail-bridge alignment are not closed by these frames. `after-bridge.png` does not show the two southern rail spans clearly enough to judge their separation, and the active geography update is still pending. Re-review those items after the map rebuild; do not infer closure from this render.

## Final bounded follow-up

The independent reviewer inspected final saved-scene hash `5c909c6b1903aecb6cd4a271951762979b66548b4b3b758c343f52e75aba41ea`. Freestanding arches, level open-air arrival, straighter LF/RF returns, and the source-supported four main bowl decks plus two exposed return banks/box floors passed this bounded review. Willis and Powerhouse marker jumps worked on the final local map.

One material defect remains: the large black foreground patch in `after-overlook.png` obscures the field-edge concourse. Material replacement and explicit triangulation experiments did not resolve it. This follow-up does not close that defect or the earlier V14 audit.
