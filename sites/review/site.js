const view=document.querySelector('#view');const version=document.querySelector('#version');const frame=document.querySelector('#comparison');
function update(){document.querySelector('#source').src=`media/source-${view.value}.jpg`;document.querySelector('#source').alt=`AECOM ${view.options[view.selectedIndex].text} reference`;document.querySelector('#model').src=`media/${version.value}-${view.value}.jpg`;document.querySelector('#model').alt=`${version.value.toUpperCase()} ${view.options[view.selectedIndex].text} model`;document.querySelector('#model-label').textContent=`${version.value.toUpperCase()} model`;}
view.addEventListener('change',update);version.addEventListener('change',update);document.querySelector('#reveal').addEventListener('input',event=>frame.style.setProperty('--reveal',`${event.target.value}%`));
for(const mode of ['wipe','side'])document.querySelector(`#${mode}-mode`).addEventListener('click',()=>{frame.classList.toggle('side',mode==='side');for(const other of ['wipe','side'])document.querySelector(`#${other}-mode`).setAttribute('aria-pressed',String(mode===other));});
